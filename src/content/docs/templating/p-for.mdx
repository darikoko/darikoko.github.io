---
title: p-for
description: A guide in my new Starlight docs site.
---
import { Aside } from '@astrojs/starlight/components';
import pythonCode from '/public/examples/p-for/example1.py?raw';
import { Code } from '@astrojs/starlight/components';


<script type="module" src="https://pyscript.net/releases/2024.11.1/core.js"></script>
<script type="mpy" src="/examples/p-for/example1.py" config="/examples/pyscript.json"></script>

PrunePy's `p-for` directive allows you to create DOM elements by iterating through a list.

You can iterate over store values too but in this examples we only iterate on raw values.

<Aside type="note">`p-for` MUST be declared on a `<template>` with **only one root element**</Aside>

Here's a simple example of using it to create a list of colors based on an array :


```html title="index.html"
<ul>
    <template p-for="color in ['red', 'blue', 'black']">
        <li p-text="color"></li>
    </template>
</ul>
```

<div class="result">
    <ul>
        <template p-for="color in ['red', 'blue', 'black']">
            <li p-text="color"></li>
        </template>
    </ul>
</div>
## Iterate on a range

```html title="index.html"  
<ul>
    <template p-for="i in range(3,7)">
        <li p-text="i"></li>
    </template>
</ul>
```
<div class="result">
    <ul>
        <template p-for="i in range(3,7)">
            <li p-text="i"></li>
        </template>
    </ul>
</div>
## Iterate on dict

```html title="index.html"
<ul>
    <template p-for="key,value in {'taste':'peperonni', 'size': 'XL'}.items()">
        <li>
            <span p-text="key"></span>
            <span> - </span>
            <span p-text="value"></span>
        </li>
    </template>
</ul>
```

<div class="result">
    <ul>
        <template p-for="key,value in {'taste':'peperonni', 'size': 'XL'}.items()">
            <li>
                <span p-text="key"></span>
                <span> - </span>
                <span p-text="value"></span>
            </li>
        </template>
    </ul>
</div>